import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emailContainer: {
    flexDirection: "row",
    borderBottomWidth: 1,
    paddingBottom: 10,
    paddingTop: 20,
  },
  input: {
    flex: 1,
    paddingLeft: 10,
  },
});

