import { StyleSheet } from "react-native";
import { MARGIN } from "./global-styles";

const styles = StyleSheet.create({
  container: {
    display: "flex",
    flexDirection: "row",
    marginLeft: "auto",
    marginBottom: MARGIN.element_margin,
  },
});

export default styles;
